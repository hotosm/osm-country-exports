# Source this file before running the sweep, or have systemd EnvironmentFile=.env.

# HDX
export HDX_API_KEY=""
export HDX_OWNER_ORG=""
export HDX_MAINTAINER=""

export OEX_S3_BUCKET="production-raw-data-api"

# export AWS_PROFILE="admin"

# DuckDB memory ceiling inside the docker / container limit, in GB.
# Leave unset to let oex derive 60% of total RAM.
# export OEX_MEMORY_GB="18"
